import colors from 'colors';
//will run first as its written in prestart, which runs before start when you do npm start(similaryly poststart)
//esw - eslint watcher
/* eslint-disable no-console */

console.log('Starting app in dev mode...'.green);
